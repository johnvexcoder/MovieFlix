"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import Link from "next/link";
import {Loader2} from "lucide-react"; import {Input} from "@/components/ui/input"; import {Label} from "@/components/ui/label"; import {Button} from "@/components/ui/button"; import {MovieFlixLogo} from "@/components/movieflix-logo";
export default function RegisterPage(){const router=useRouter();const [form,setForm]=useState({username:"",fullName:"",dateOfBirth:"",contactNumber:"",email:"",password:""});const [error,setError]=useState("");const [loading,setLoading]=useState(false);const field=(key:keyof typeof form)=>(e:React.ChangeEvent<HTMLInputElement>)=>setForm({...form,[key]:e.target.value});
async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)}),d=await r.json();if(!d.success)throw new Error(d.error);sessionStorage.setItem("movieflix_signup_token",d.data.signupToken);router.push("/register/payment")}catch(e){setError(e instanceof Error?e.message:"Registration failed")}finally{setLoading(false)}}
return <main className="cinematic-bg min-h-screen px-4 py-10 text-white"><div className="mx-auto w-full max-w-xl rounded-3xl border border-cyan-400/20 bg-[#0b1629]/95 p-6 shadow-2xl sm:p-9"><div className="mb-7 text-center"><MovieFlixLogo className="mx-auto h-16 w-16"/><h1 className="mt-3 text-3xl font-black">Create your account</h1><p className="mt-2 text-sm text-slate-400">Choose your plan after registration.</p></div><form onSubmit={submit} className="grid gap-4 sm:grid-cols-2">{[["username","Username","text"],["fullName","Full name","text"],["dateOfBirth","Date of birth","date"],["contactNumber","Contact number","tel"],["email","Email address","email"],["password","Password","password"]].map(([k,l,t])=><div key={k}><Label htmlFor={k}>{l}</Label><Input id={k} type={t} value={form[k as keyof typeof form]} onChange={field(k as keyof typeof form)} required className="mt-1.5 h-12 border-white/15 bg-white/5" autoComplete={k==="password"?"new-password":undefined}/></div>)}{error&&<p className="sm:col-span-2 rounded-xl border border-red-400/30 bg-red-950/40 p-3 text-sm text-red-200">{error}</p>}<Button disabled={loading} className="btn-brand h-12 sm:col-span-2">{loading&&<Loader2 className="mr-2 h-4 w-4 animate-spin"/>}Continue to plans</Button></form><Link href="/login" className="mt-5 block text-center text-sm text-cyan-300 hover:underline">Already registered? Sign in</Link></div></main>}
