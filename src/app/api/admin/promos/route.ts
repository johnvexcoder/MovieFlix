import { asc,eq } from "drizzle-orm"; import {v4 as uuidv4} from "uuid"; import {db} from "@/db"; import {promoCodes,subscriptionPlans} from "@/db/schema"; import {verifyToken} from "@/lib/auth"; import {successResponse,errorResponse} from "@/lib/api-response";
async function admin(r:Request){const token=(r.headers.get("cookie")||"").match(/(?:^|; )admin_token=([^;]+)/)?.[1];return token?verifyToken(decodeURIComponent(token)):null}
export async function GET(r:Request){const a=await admin(r);if(!a?.isAdmin)return errorResponse("Admin access required",403);return successResponse({promos:await db.select().from(promoCodes).orderBy(asc(promoCodes.code)),plans:await db.select().from(subscriptionPlans).orderBy(asc(subscriptionPlans.sortOrder))});}
function values(b:Record<string,unknown>){const code=String(b.code||"").trim().toUpperCase(),discountAmount=Number(b.discountAmount||0),bonusHours=Number(b.bonusHours||0),maxUses=b.maxUses?Number(b.maxUses):null;if(!/^[A-Z0-9_-]{3,32}$/.test(code)||discountAmount<0||bonusHours<0||(maxUses!==null&&maxUses<1))return null;return{code,description:String(b.description||"").trim()||null,discountAmount,bonusHours,forcedPlanId:b.forcedPlanId?String(b.forcedPlanId):null,newUsersOnly:Boolean(b.newUsersOnly),birthdayMonthOnly:Boolean(b.birthdayMonthOnly),maxUses,startsAt:b.startsAt?String(b.startsAt):null,expiresAt:b.expiresAt?String(b.expiresAt):null,isActive:b.isActive!==false}}
export async function POST(r:Request){try{const a=await admin(r);if(!a?.isAdmin)return errorResponse("Admin access required",403);const v=values(await r.json());if(!v)return errorResponse("Invalid promotion values",400);const now=new Date().toISOString(),id=uuidv4();await db.insert(promoCodes).values({id,...v,uses:0,createdAt:now,updatedAt:now});return successResponse({id},201)}catch(e){console.error(e);return errorResponse("Code already exists or could not be saved",400)}}
export async function PATCH(r:Request){try{const a=await admin(r);if(!a?.isAdmin)return errorResponse("Admin access required",403);const b=await r.json(),v=values(b);if(!v||!b.id)return errorResponse("Invalid promotion values",400);await db.update(promoCodes).set({...v,updatedAt:new Date().toISOString()}).where(eq(promoCodes.id,String(b.id)));return successResponse({message:"Promotion updated"})}catch(e){console.error(e);return errorResponse("Unable to update promotion",400)}}
export async function DELETE(r:Request){const a=await admin(r);if(!a?.isAdmin)return errorResponse("Admin access required",403);const id=new URL(r.url).searchParams.get("id");if(!id)return errorResponse("Code ID required",400);await db.delete(promoCodes).where(eq(promoCodes.id,id));return successResponse({message:"Promotion deleted"})}
