import { errorResponse,successResponse } from "@/lib/api-response";import { quote,resolveCustomer } from "@/services/billing/service";
export async function POST(request:Request){try{const body=await request.json(),customer=await resolveCustomer(request,body.signupToken);if(!customer)return errorResponse("Authentication required",401);const q=await quote(customer,String(body.planId||""),body.promoCode);return successResponse({plan:{id:q.plan.id,name:q.plan.name,durationHours:q.plan.durationHours,isLifetime:q.plan.isLifetime},promo:q.promo?{code:q.promo.code,description:q.promo.description}:null,originalAmountMinor:q.originalAmountMinor,discountAmountMinor:q.discountAmountMinor,finalAmountMinor:q.finalAmountMinor,currency:q.currency})}catch(e){return errorResponse(e instanceof Error?e.message:"Could not calculate checkout",400)}}
